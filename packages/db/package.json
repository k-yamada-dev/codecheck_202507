{
  "name": "@acme/db",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "prisma"
  ],
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "postinstall": "prisma generate",
    "build": "tsc -b",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:status": "prisma migrate status",
    "migrate:dev": "prisma migrate dev",
    "migrate:generate": "prisma generate",
    "seed": "prisma db seed",
    "reset": "prisma migrate reset -f --skip-generate"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "6.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "6.14.0",
    "typescript": "^5.5.4"
  }
}
